server:
    port: 8080
    
spring:
    data:
        mongodb:
            uri: ${MONGO_DB_URI}
            
multipart:
    maxFileSize: 5MB
    maxRequestSize: 5MB

# ------ 以下為自訂設定 ------
mail:
    username: ${MAIL_USER}
    password: ${MAIL_PASSWORD}
    
store:
    file:
        path: /Users/tommyyeh/Documents/workspace/TogetherWs/user-files